defmodule ExplorerWeb.DataJSON do
  def show(
    %{
      amount_of_proofs: amount_of_proofs,
      fee_per_proof: fee_per_proof
    }
) do
    %{
      amount_of_proofs: amount_of_proofs,
      fee_per_proof: fee_per_proof
    }
  end
end
